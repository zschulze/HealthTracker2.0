<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zachschulze.healthtracker20" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ui.activities.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.AddFoodItemActivity"
            android:label="@string/title_activity_add_food_item" >
        </activity>
        <activity
            android:name=".ui.activities.AddMealItemActivity"
            android:label="@string/title_activity_add_meal_item" >
        </activity>
    </application>

</manifest>
